/**
 * Event Types
 * 
 * This module exports an array of objects representing different types of sports events.
 * Each object contains a `value` and a `label` property used for selection and display purposes.
 * 
 * @module eventTypes
 */

const eventTypes = [
  { value: 'football', label: 'Football' },
  { value: 'basketball', label: 'Basketball' },
  { value: 'swimming', label: 'Swimming' },
  { value: 'tennis', label: 'Tennis' },
];

export default eventTypes;
